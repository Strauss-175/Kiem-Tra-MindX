const numberOneTriangle = parseInt(prompt( "nhap so tam giac"));
for (let i = 0; i <= numberOneTriangle; i++)
{
    const tri = document.createElement("triangle");
    tri.innerText=("")
    d
}